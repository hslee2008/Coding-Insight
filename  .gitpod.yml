tasks:
  - name: 'Nuxt.js (Py KR)'
    init: cd vue/python-kr && npm install
    command: cd vue/python-kr && npm run build && npm run generate && npm run dev
    openMode: split-right
  - name: 'React Native'
    init: cd app/Python-Factory && npm install
    command: cd app/Python-Factory && npm run start
ports:
  - port: 8000
    onOpen: open-preview

vscode:
  - extensions:

github:
  prebuild:
    master: true
    branches: true
    pullRequests: true
    pullRequestsFromForks: true
    addComment: true
    addCheck: false
    addBadge: true

gitConfig:
  user.email: "hyunseunglee2008@gmail.com"
  user.name: "HyunseungLee-Travis"
  alias.acp: "!f() { git pull && git add -all && git commit -a -m \"$@\" && git push; };"
